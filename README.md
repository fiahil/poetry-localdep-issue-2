# poetry-localdep-issue

Requires: Python3.8 and Poetry 1.1.4

This repo serves as an example that nested poetry project dependencies are
not being properly resolved. To see this:

```sh
cd apps/app1
poetry update
```
